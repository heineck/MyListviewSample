<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heineck.mylistviewsample" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".core.InitialActivity"
            android:label="@string/title_activity_initial" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".swipelistview.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".buttoninsidelistview.ButtonInsideListViewActivity"
            android:label="@string/title_activity_button_inside_list_view" >
        </activity>
    </application>

</manifest>
